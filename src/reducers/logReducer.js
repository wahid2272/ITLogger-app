const intialState = {
  logs: null,
  current: null,
  loading: false,
  error: null
}

export default (state = intialState, action) => {
  switch(action.type) {
    default:
      return state;
  }
} 
